const base_url = "http://localhost:3000";

const connect_miniserver = `${base_url}/api/connect-miniserver`;

const structure_url = `${base_url}/api/get-structure`;

export { connect_miniserver, structure_url };
